# Conferral — after r09d: a scope error, and a spend freeze until 2026-09-01

**Status: work is STOPPED at r09d's frozen consequence.** S1'' read INCONCLUSIVE under its
own pre-declared coldness clause, and the second pass it mandates was interrupted by an
account-level API limit. Run 14 was not fired. Full reading:
`docs/unification/reports/r09d-entity-anchor.md`.

## The operational fact that reshapes the plan

**The Anthropic account has reached its usage limit; access returns 2026-09-01 00:00 UTC.**
Consequences, none of them a judgement call:

- **Run 14 (~$40) cannot be fired before that date**, whatever any sweep reads.
- **No cold row can be warmed** before that date, so the readable set cannot be improved —
  and r09d's S1'' failed *only* because 3 of the 4 known-wrong rows were cold.
- Everything that stays possible is **$0**: replays over warm cache, censuses, splices,
  instrument work, and reading.

## What r09d established

| criterion | read | verdict |
|---|---|---|
| S1'' — every readable wrong row flips | 1 of 4 readable (q2-018, still wrong) | **INCONCLUSIVE** → mandated pass 2 → **interrupted by the API limit** |
| S2'' — collateral ≤ 5 | 2 | PASS |
| S3'' — blocking row repaired | q2-011 reports the gold | PASS |
| S4'' — q2-009 and q2-046 return | q2-009 yes, q2-046 no | **FAIL** |

**D3 (the S2 join) is confirmed** on its one readable target: q2-009 returned to correct and
no row lost a correct commit to the join. **q2-046 was never S2's row** — its damping is the
pre-existing competition term (n_comp = 12) and the anchor does not fire there at all; r09c
mis-attributed all three of its collateral rows to S2, and that is corrected in the report.

**The decisive result is a $0 census of the lever itself**, run because the criteria could
not resolve:

| window | fires | damps the gold | damps the gold while a non-gold stays at 1.0 | clean firings |
|---|---|---|---|---|
| **±120 quote window (as built)** | 42 | 26 | **12** | 16 |
| ±600 local | 43 | 26 | 10 | 17 |
| **whole chunk (document-scoped)** | 28 | 13 | **1** | 15 |

The frozen window is refuted, with the mechanism read off the wire: on one row the five
discriminating terms are tokens of the document's subject; the gold's ±120 window carries the
value but none of them, while a competitor's window carries all five — so the rule damps the
gold and leaves the competitor alone. **A value-adjacency window cannot carry a
document-level qualifier.** Run 9's window is right for competing values, which must be
adjacent; wrong for an entity anchor, which need not be.

## Options

**A — rescope the window to the document and re-census, all $0 (recommended).** One frozen
constant changes: the anchor reads the whole chunk, the competition term keeps run 9's ±120.
Priced above: strictly harmful on 1 row instead of 12, at the same clean-firing count. Then
re-run the $0 census as the acceptance bar, and hold the sweep + run 14 until access returns.
*Cost:* one short build session, $0 measurement. *Risk:* the census is a proxy for the gate,
not the gate — it counts firings, not commits.

**B — keep the anchor as built and read the sweep again after 2026-09-01.** Buys nothing the
census has not already answered, and the census says the current constant is net-harmful.

**C — drop the anchor, keep D3 (the S2 join) alone.** D3 is the confirmed half. It fixes no
wrong commit, so it cannot unblock run 14 by itself, but it is a clean, measured improvement.
*Cost:* $0. *Consequence:* the §6.12 block stands with no lever aimed at the blocking class.

**D — park r09d entirely** and revisit after access returns.

**The census becomes an instrument either way.** It is a $0 pre-sweep filter that found a
scope error the gate would have charged ~$40 to reveal — worth registering as standing
practice: *price a lever's firing direction on the whole battery before buying a sweep.*

## Recommendation

**A, with the census frozen as its acceptance bar, plus C's half kept** — rescope the
window, re-census, and let the corrected criteria wait for access. The interval to
2026-09-01 is exactly the kind of work that costs nothing: instrument, measure, and warm what
can be warmed at $0.

## Questions

1. **The window:** A (rescope to document-scoped and re-census), B (keep as built and re-read
   after 2026-09-01), C (drop the anchor, keep the S2 join), or D (park r09d)?
2. **The acceptance bar for a rescoped anchor:** the $0 census (strictly-harmful rows ≤ 2 and
   clean firings ≥ 15), or a sweep only, once access returns?
3. **The interval to 2026-09-01:** spend it on $0 instrument/measurement work as above, or
   hold everything until access returns?

## RULINGS (owner, interviewed 2026-08-24)

1. **Option A — the anchor is rescoped to the DOCUMENT.** The anchor reads the whole chunk;
   the competition term keeps run 9's ±120 (it is right for competing values, which must be
   adjacent). One frozen constant changes; everything else in r09d stands, including D3.
2. **The $0 census is the acceptance bar, frozen now:** a rescoped anchor is accepted only at
   **strictly-harmful rows ≤ 2** and **clean firings ≥ 15**, measured on the whole battery
   before any sweep is bought. Registered as standing practice — *price a lever's firing
   direction on the whole battery before buying a sweep.*
3. **The interval to 2026-09-01 is spent on $0 work:** rescope, re-census, warm what warm
   cache allows, and leave the sweep + run 14 queued to fire when access returns.
