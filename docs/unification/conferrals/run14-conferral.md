# Run 14 — the firing conferral

**Status:** 2026-08-25, for the owner interview. Every number below is measured at $0 from
run 13's record; the counterfactuals were run BEFORE this document was written. Pass 2 of
the master counterfactual is still in flight; it can only refine option C (already dominated
on pass-1 + run-11 evidence) and its outcome is recorded in an addendum below the RULINGS —
it cannot change the recommendation.

## The question

Run 13 FAILED on two of ruling 4's three conjuncts and its FAIL branch was enacted (JOIN
reverted from master, §6.12 block stands, STOP). The temper arc and its successors closed
with one surviving decision-path change (D3) and one refused lever (E1). **No candidate tree
reads clean under run 13's conjuncts verbatim, so run 14 cannot be fired as a re-run: the
conferral must pick a tree and freeze what its wrong-commit conjunct is measured against.**

## Evidence — the measured landscape (all $0)

**The parked tree** (JOIN + T1 + A1 + A2 + D3; branch `r10-entity-key` post-E1-revert,
tree-identical to the pre-E1 head):

- r09e/r10 sweeps of run 13's record (66–68/104 readable): **the blocking row commits
  CORRECT**; the warm-deliberate row is repaired wrong → withheld; **two rows still commit
  wrong** — the corroborate-tier row and the entity-qualifier row (its only working repairer,
  E1, was refused by its own frozen bar; the carrier-side lever family is closed). Two
  temper-stack collaterals move correct → withheld (attributed away from D3 by isolation).
  **Both standing wrong rows were already wrong in run 13's typed arm — the parked tree
  produces zero wrong commits that run 13 did not have.**
- **The splice** (`gate_splice.py`; the pin reproduces run 13's published 0.895 / +0.424
  exactly before anything is spliced): applying only the three measured row changes to run
  13's paired rows reads **PASS — P(Δ>0.05) = 0.940, Δ̄ +0.492 [+0.024, +0.996]**, answer
  rate 0.68, typed spend $0.58 vs mono 0.97 / $39.01. Artefact:
  `eval/gate-outside-option/run14-splice-parked.md` (KB).

**Master** (JOIN reverted; the run-11-class tree + r08 + M1.5):

- Replay pass 1 (46/104 readable; pass 2 in flight to grow the readable set): **seven rows
  the record answered correctly collapse to withheld at n_obs = 1** — the §6.12
  replace-branch erasure, measured live on this record; the warm-deliberate wrong is avoided
  only by abstaining. The blocking row and all four named wrong rows are cold on pass 1.
- Prior priced evidence: run 11 (master-like tree) read FAIL 0.880 with the blocking row
  committing wrong — the exact class the §6.12 block exists for.

**The calendar:** the Anthropic key is usage-limited until 2026-09-01 00:00 UTC (verified
live 2026-08-25). The refusal message is the *self-configured monthly limit* class — raising
the cap in the console lifts it immediately. The entire remaining priced tail is **≤ $10**:
warm ~$1–2, run 14 ~$1–5 (typed arm only; the mono arm is archived and is not re-fired).

**Residual unknowns, named:** the superset-confirm row is cold on every pass (already wrong
in run 13, so it cannot become a *new* wrong; its repair is not claimed either); the §6.13
commit-wobble floor is 2 rows; the unreadable rows shrink to near-zero after the warm pass.

## Options

**A — fire on the parked tree; conjunct baselined on run 13 (recommended).** Frozen
branches, full delegation as in ruling 4:
PASS = (1) P(Δ>0.05) ≥ 0.90 at the frozen δ/level (0.05 / 0.90) ∧ (2) the blocking row
commits correct ∧ (3) **zero NEW wrong commits — "new" = a row that was not wrong in run
13's typed arm** ∧ (4) the standing hard clause (no named wrong-commit class worse).
PASS ⇒ close the §6.12 block, merge the parked tree to master, deploy to live, no further
keypress. FAIL on any conjunct ⇒ STOP for a ruling, nothing merges.
Expected on the splice: PASS all four. Price: ~$3–7 with the warm pass. Risk: the two
standing wrongs ride into production behavior — explicitly priced (they are dispersal
conversions; the mono arm answers 0.97 with far worse EU), not hidden.

**B — require zero wrong commits absolutely.** Measured FAIL in advance on every candidate
tree: the parked tree carries two standing wrongs; master carries the blocking row. Coherent
only as "do not fire" — it converts the gate into a wait for a lever class that three
refuted checkpoint families say does not exist on this corpus.

**C — fire on master.** Dominated on both conjuncts that matter: the blocking row is
unrepaired (run 11, and the block's own class) and the reach collapse is measured (seven
rows on this record alone). Not recommended at any price.

**D — park the arc; no deployment.** The status quo, indefinitely. The block's purpose was
to gate deployment on evidence; the evidence now exists and points at A.

## Sequencing (either calendar)

Warm the cold rows first (`warm-rows.sh`, rehearsed, ~$1–2) → re-read the record at $0
(shrinks the unknown set to the wobble floor) → fire run 14 under the frozen branches.
If the cap is raised: all of it the same day. If not: 2026-09-01.

## Recommendation

**A**, warm-first, at whichever date the cap allows.

## Questions for the owner

1. **Tree + conjunct:** A / B / C / D?
2. **The "new wrong commit" baseline:** run 13's typed arm (the four named rows) — yes/no?
3. **The cap:** raise it now (fires this week) or hold to 2026-09-01?
4. **Delegation on PASS:** merge parked → master and deploy to live without a further
   keypress (ruling-4 style), or stop for a final look first?

## RULINGS

*(recorded after the interview; nothing above this line changes.)*

**Taken 2026-08-25 (owner, interviewed after the conferral was committed at `be7fa14`):**

1. **Option A.** Run 14 fires on the parked tree (branch `r10-entity-key` post-E1-revert,
   tree-identical to the pre-E1 head) under the four frozen conjuncts: (1) P(Δ>0.05) ≥ 0.90
   at the frozen δ/level 0.05 / 0.90; (2) the blocking row commits correct; (3) zero NEW
   wrong commits; (4) the standing hard clause — no named wrong-commit class worse.
2. **Conjunct 3's baseline is run 13's typed arm.** A wrong commit is NEW iff the row was
   not wrong in run 13's typed arm. The two standing wrongs are carried, priced, published.
3. **The cap is raised now** (owner console action). On restored access, same day:
   warm the superset-confirm row (`warm-rows.sh`, the ruled targeted scope) → re-read the
   record at $0 and update the splice → fire run 14 under the frozen branches.
4. **Full delegation on PASS:** close the §6.12 block, merge the parked tree to master,
   deploy to live, publish — no further keypress. FAIL on any conjunct: STOP for a ruling,
   nothing merges.


## ADDENDUM — the master counterfactual's second pass (2026-08-25, $0)

Pass 2 (fresh staging from the live store, the mandated double run) reproduces pass 1
**byte-for-byte at the decision layer**: the same 46/104 readable, the same fidelity 38/46,
the same eight moved rows — seven correct → withheld collapses at n_obs 1 and the
warm-deliberate wrong avoided only by abstaining — and the blocking row plus all four named
wrong rows cold on both passes. Two consequences, both stable across the double run:
master's replace-branch reach collapse is confirmed, and **the blocking row is unreadable at
$0 on master** (its priced evidence remains run 11: FAIL 0.880 with the blocking row
committing wrong). Option C's description is final; the recommendation is unchanged.


## RULING 5 — direction (owner, 2026-08-25, after the addendum)

**Proplang is the intended successor of credence at the decide seam — and credence ships
first.** Run 14 proceeds as ruled on the credence-typed arm; the proplang engine (today the
membrane shadow, never on the decision path) replaces credence as its own successor
milestone, which requires its own gated adoption (shadow → challenger → a priced run under
frozen criteria), not a swap-by-fiat. Nothing in this arc presupposes the swap; everything
in it — the wire, the ledger, the record/replay instruments, the gate — binds to the decide
seam, not the engine, and carries over. The owner also rules the API cap raised (ruling 3's
first branch): on restored access the ruled tail runs — warm → re-read at $0 → fire run 14
under the frozen branches, full delegation on PASS.
