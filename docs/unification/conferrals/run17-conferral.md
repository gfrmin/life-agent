# Conferral — run 17 FAIL (M5, r15): what to do with the argmax absorption

> Prepared before the interview (the standing convention): the evidence, the options,
> and each option's price. Run 17 (`gate-20260826T025059`) FAILED conjuncts (a) and
> (b); the warm-deliberate class read worse than run 16 (c is in play for any deploy).
> Master is untouched — M5 lives on `collapse-m5`, unmerged; live runs M4.

## The evidence in one paragraph

M5's five moves landed green through every cheap gate. The priced gate then read
0.743 / Δ̄ +0.238 (run 16: 0.961 / +0.519) with the answer rate down 0.62 → 0.49 and
dispersed withholds up 37 → 51. The attributable mechanism is ONE move: A2's fix (the
grow offer after every terminal), which the $0 probe had shown flips 62/63 recorded
reports into scheduled re-reads. Live, the engine exercised that reach (deliberate
firings 41 → 71, $4.51), the bought evidence joined the channel, and marginal reports
dispersed into withholds. The engine's hand-set grow priors price information above
its realised Δ-worth. Everything else in M5 (the lane/gather/B-4 deletions, the
dispatch absorption + terminals-only regime, the leaf regime declaration, L-3's
engine-picked scoped rows, D-5) has NO evidence against it — but also no separate
priced reading, because run 17 carried all of it together.

## Options

**A — Revert A2's fix alone; keep the rest of M5; re-register; re-run (recommended).**
The probe measured the residue honestly and the gate priced it: real, and harmful
under the current engine calibration. Reverting the widened offer (one commit:
restore the withhold-only re-ask condition) returns the decision surface to run 16's
on that axis while keeping every other M5 move. The A2 amendment's frozen consequence
("the fix lands") was honoured — landed, priced, and now the gate's own FAIL branch
disposes of it. Price: a small pre-registered revert + run 18 ≈ $1–5. Risk: if the
FAIL has a second cause hiding behind E-12, run 18 fails too and says so cleanly
(the isolation reading the run-10→12 ladder pattern wants).

**B — Keep the fix; re-calibrate the grow pricing before re-running.** The engine's
recovery priors are hand-set (the "hand-priced VOI" the census wanted retired); the
gather-outcome stream now holds hundreds of rows that could ground them. Cross-repo
(credence) work under its own pre-registration; the M5 checkpoint stays open
meanwhile. Price: an engine arc (days), then a run. This is the principled fix if the
owner wants the argmax to genuinely own recall — but it holds M5 hostage to an
engine-calibration arc.

**C — Park M5 entirely.** The branch stands; the ladder resumes later. Price: none
now; the collapse programme stalls at M5 and the M6/M7 sequence waits.

**Orthogonal, either way:** the warm-deliberate wobble row (conjunct b's NEW-by-letter
FAIL) has now flipped disposition four times across identical trees. If the ruling
wants conjunct (b) robust to it, the clause needs a class-based restatement at the
NEXT pre-registration (name the class + the bar + the cold-row consequence — the
registered r10 lesson), never a retroactive edit.

## What does NOT change under any option

The two standing wrong rows ride in production exactly as before (master/live is M4's
tree; run 17 changed nothing there). The live calibration ledger gained run 17's rows
under its `run_id` (accounted, as every gate run since 14).
