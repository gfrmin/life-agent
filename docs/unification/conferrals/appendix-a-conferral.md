# Conferral — Appendix A: the PRINCIPLES amendment (the M7 signature)

> Prepared 2026-08-26 for the owner's keypress. The signature is the LAST act of the
> collapse ladder (design §8's M7 row: "Appendix A signed here, not before — the
> constitution changes when the structure exists"). Everything below it — M2–M7's
> code — is merged and deployed; the signature changes only the constitution's text.

## What signing enacts (three verbatim amendments, design Appendix A)

- **A.1 — PRINCIPLES §16 gains the three-verdict rule**, added as a paragraph after
  the sentence ending "…truth is the fold." — every mechanism outside the argmax is
  belief-shaping (declared error model), decision-shaping (into the argmax or dies),
  or mechanics (enactment, recorded never priced); no fourth verdict; the named
  exceptions live once in the design's §6.
- **A.2 — PRINCIPLES §15's spine sentence is replaced** with the stronger form: the
  spine's mechanics are *recorded* on the decision's record and never *priced* by
  it, and nothing in the spine may prefer one decision space or one evidence policy
  over another when both are available — which space/policy a decision was ranked
  under is a fact on its record, not a host choice.
- **A.3 — foundations §14 gains the resolved-decision entry** "The module collapse
  is adopted (date of signature)": one decision site, one `posterior()` with two
  declared evidence policies, one utility atom, one price table, one poster.

The verbatim texts are in `docs/module-collapse-design.md` Appendix A — they are
what gets pasted, not paraphrased; A.3's date is the signature date.

## The evidence the signature rests on

The structure the amendments describe EXISTS, gate by gate: M2 (one poster, r12) ·
M3 (one fold entry point, r13, run 15 PASS) · M4 (one atom + price table, r14, run
16 PASS 0.961/+0.519) · M5 (one argmax surface, r15, run 18 PASS 0.959/+0.514
after the A2-revert ruling) · M6 (the observation model declared once, r16, 314/314
pure equality) · M7's code half (the §6 register pinned by a mutation-verified
re-listing guard; D-4/D-6 closed as derived views; r17). Live = master, deployed on
steel. The two standing wrong rows ride priced and published; the §14 ledger
carries the open arcs (hand-priced VOI; the volatility latent) with owners.

## Price and the decline branch

Signing: $0, docs-only, one owner-authorised commit (say the word and I enact the
three amendments verbatim and stamp the date). Declining: the structure stands and
behaves identically; the constitution keeps its pre-collapse §15/§16 text; the
completion programme's DONE item 1 ("the owner has signed Appendix A at M7")
re-opens as an owner item — nothing else blocks on it.

## RULING

_(recorded after the keypress)_
