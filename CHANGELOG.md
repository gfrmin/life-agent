# Changelog

- 2026-09-19 (J0, reset): unification arc archived (tag `archive/unification-arc-v0`; registers, reports, conferrals, 60+ audit instruments and their tests under `archive/`); no behaviour change on the ask path.
- One scoreboard (`eval/score.py` → `SCOREBOARD.md`, sets pinned by sha256, rule 5 gate); owner board reproduces run 18: typed 61/2/41, oracle 95/6/3, router 97/5/2 at $15.97 vs $39.01.
- `make check` (ruff + pytest -n, 71 s, green with no KB) / `score` / `score-quick` / `data` / `sets` / `engine`; `config/engine.lock` pins proplang at `94fd4eb`. New dev dependency: pytest-xdist.
- Entry docs replaced: one-page `CLAUDE.md`, `MODEL.md`, J0–J5 `ROADMAP.md`, `KICKOFF.md`; PRINCIPLES §1/§14 name proplang as the brain. Production readout drops its governance-register line.
- 2026-09-19: proplang pinned to its first release, `doctrine-sitting-r1` (= `94fd4eb`, sha256 verified); `make engine` downloads it with curl (no gh auth needed) and refuses a mismatched sha. The ER core is its own public repo (hkaddresses#27); tannen `m5-close` is J2's pin.
- 2026-09-19: J1 steps 1–2. The candidate posterior (`core/posterior.py`), the utility folds and the reliability Betas are local Python, each pinned to the Julia engine's recorded outputs (605 decides, 23 boot Ūs, 102 opening ρ). Channel constants live once in `pricing.py`. Board unchanged. The scope proplang can't take is on record in proplang#29.
- 2026-09-19: J1 steps 3–4. The decider is the host Bayes act: `/decide` runs the local posterior, then `core/decide.bayes_act` (abstain/gather/ask/respond at the MAP credence), and `core/enact.py` enacts it. Gather is priced from recorded gather sequences, one row per gathers-already-applied (`core/gather_row.py`, 385 m5-base steps; the pooled 0.093 grow rate it replaces cost 24 right answers on the board), ask at a measured recovery rate (it had been priced as perfect information). The rerank is a recorded, metered derivation at its measured $0.047 (it was uncached, unmetered and declared at $0.004). proplang, tannen and the ER core are doors after the MVP; `membrane/` stays green off the path.
- Deleted: the credence skin (`core/brain.py`), the narrative and synthesis lanes, `terminals.answer`, the interval rows, `ledger/golden.py`, and the scripts that drove them (bar_audit, verdict, triage_answers, the synthesis arm). Recorded narrative verdicts still fold.
- Rule 5 is expected ΔU (`eval.score --gate`); the board gains U/q and is graded by exact match (run 18 re-graded: oracle 96/5/3). Owner board, host act vs run 18 at the folded gauge: typed 48/0/56 (ΔU −2.2 as recorded, +0.3 with run 18's unmetered reranks), router 99/3/2 (ΔU +5.4). Rules 1–2 restated: the loss is stated and the bar derived; one argmax.
- 2026-09-20: the gather menu measured per option (how often each lifts the leader and by how much: corroborate_sonnet +0.16, retrieve_expand −0.12 — retrieval adds candidates and disperses) and ranked on it rather than on price. Built, measured, REVERTED at rule 5: typed 56/2/46 (U 45.7 vs 48.0) and router 97/5/2 (U 48.0 vs 55.5). It exposed the binding defect — a decline falls through to the oracle, so the act's alternative to responding is worth ~+0.9, not the 0 `abstain` is priced at, and it commits too eagerly. J2's escalate rows put that alternative on the menu; re-measure there.
- 2026-09-20: the board's two arms were never on one grader — the typed arm is graded on the span it asserts, the outside option on whether the gold appears anywhere in its prose (on the same 87 live answers: 0.805 vs 0.897). Repinned to one grader (`scripts/regrade_outside_option.py`, 104 rung answers at the corpus pin, 17 bought for $7.94): outside 96/5/3 → 87/13/4, router U/q +0.48 → +0.10, typed unchanged at +0.48. So the typed row is the MVP target, not the router, and the escalate row does not pay at this gauge (−0.50 per escalation; −0.04 even free; +0.01 at its best self-report bar). J2 keeps origin and disclosure; a rung ships when it beats abstaining. `core/outcome_mixture.py` now holds the one estimator both measured rows use.
- 2026-09-20: the board prices the act, not the cache. Run 18's typed arm had been priced at what its warm replays metered ($0.37) while calling the deliberative rung on 40 of the 104 at $0.38 a call; every typed row is now priced at the menu's declared prices for the probes it applied (`pricing.list_price`; `applied` and `metered_usd` ride on the archive; `scripts/recost_paired.py` re-prices an archived run from its A-loop record). On one grader and one price list run 18 reads typed 61/2/41 at $20.95, U/q +0.220 (was +0.483).
- 2026-09-20: J1's typed arm pinned as the owner set: the host act 48/0/56 at $1.44 (at most three gathers, never the rung), U/q +0.443 — ΔU +23.2 against the daemon on the typed row and +18.0 on the router row, the outside row unchanged. The daemon's 13 extra right answers rode with its 2 wrongs and 40 rung calls; at today's gauge that bundle is worth less than abstaining. The 15 rows that differ are listed in PR #192.
- 2026-09-20: the generated golden set is on the board as set `generated` (kind `typed`): 212 verbatim point-fact questions generated from the corpus (`make golden`; haiku, prompt sha `ea86a53553e58005`), answered COLD by the host act (no derivation cache; run on a second machine at the corpus pin): 85/4/123 at $0.015 a question list price, U/q +0.284 — the baseline, no incumbent. The four wrongs committed at 0, 1, 3 and 3 gathers. Cold, the probes meter more than the menu lists: $12.85 metered against $3.20 at list for the 212, +$0.046 a row, growing with gathers applied (the first extraction on zero-gather rows and the re-extraction inside expand/corroborate are unpriced) — the menu is the owner's, no price changed.
- 2026-09-20 (J2, origin): every reply's first line names where the answer came from — "From your documents.", "Answered by the <rung> rung; N retrieved document(s) were shared with it.", or "Declined: <reason>." — derived once (`decisions.origin`) from the decision's effector, the asserted value and the instrument that proposed it; the decision record is v4 with an `origin` field (older lines read as silence). Disclosure rows wait with the rung.
- 2026-09-20 (J4, the dead-man): `scripts/production_readout.py` exits 1 when its own window flag says STALE, so the weekly timer's wrapper turns a stopped stream into a monitor `/fail` — closing the gap `docs/guards.md` row 12 named in advance ("nothing reads the production readout"). An empty stream is stale too; `--allow-stale` reads by hand; the absent-root failure keeps its own code. New `scripts/live_archive.py` + `make live` / `make live-archive`: the live stream as a board-shaped archive, joined to the owner's verdicts on `decision_id` (never `question_id`), eval traffic excluded. An assert nobody graded is written `censored` — recorded, not scored; a withhold scores as a decline whatever the owner said about it, and the summary states those separately. Read today: 1882 live rows, last 2026-08-31, 20/1/1538, 45 withholds the owner wanted answered. Not pinned yet — the row waits for a week of use.
- 2026-09-20: the declined-route reply is now the origin line itself (`lookup.origin_line`), not a second hand-written sentence that happened to start with "Declined:" — rule 3 by construction on the one path that renders its own text. jarvis loses the unreachable terminals-only branch and `DriveResult` its dead `text` field, both orphaned when J1 retired the in-process lanes.
- 2026-09-20 (J3, the stranger): the documented path now reaches a decision — the bridge is in `SETUP.md`, `make data` completes the ingest, `make sets` fetches ATM-Bench at a pinned revision (CC-BY-NC, cached outside the tree), and the fresh-clone smoke boots the real bridge with a tripwire for a client and asserts `/ready` is the host act, `/decide` commits and withholds, and both origin lines render — keyless. The defect it exposed was the decider's: with no fitted gather row the uniform prior makes gathering never pay, so a fresh install declined 14 of 14 on the bundled corpus; `config/gather-row.example.json` ships the fitted row as the default (a KB's own fit still wins) and the same 14 read 8/0/6. Two rows pinned — `sample` 8/0/6 (+0.556; questions and archive in the repo, `root: repo`) and `atm` 26/5/167 (−0.012; 198 of ATM-Bench's 381, its own KB root via `root_env:`). `atm`'s U/q is a floor: all five wrongs were re-asked and none invents — each names the gold in another surface form, and only 77 of the 198 golds are verbatim spans in their cited document, so re-cutting the set on that precondition is the owner's call. `SCOREBOARD.md` now prints each scored row's note under the table.
- 2026-09-21 (K1): `make data` reads every extractor version before it starts. pkm's guard is correct but per-producer and lazy — it constructs a producer only when a source first routes to it — so an upgrade that moved three of the five declared versions (docling, unstructured, tesseract) cost one full pass over the corpus per producer to discover, each abort landing after earlier work had committed. `ingest_sources.py --extract` now preflights the config against what is installed and names every drifted producer at once (`--no-preflight` opts out); the versions were reconciled by declaring what is installed, which re-extracts and invalidates nothing because routing reads successes version-blind.
