# Changelog

- 2026-09-19 (J0, reset): unification arc archived (tag `archive/unification-arc-v0`; registers, reports, conferrals, 60+ audit instruments and their tests under `archive/`); no behaviour change on the ask path.
- One scoreboard (`eval/score.py` → `SCOREBOARD.md`, sets pinned by sha256, rule 5 gate); owner board reproduces run 18: typed 61/2/41, oracle 95/6/3, router 97/5/2 at $15.97 vs $39.01.
- `make check` (ruff + pytest -n, 71 s, green with no KB) / `score` / `score-quick` / `data` / `sets` / `engine`; `config/engine.lock` pins proplang at `94fd4eb`. New dev dependency: pytest-xdist.
- Entry docs replaced: one-page `CLAUDE.md`, `MODEL.md`, J0–J5 `ROADMAP.md`, `KICKOFF.md`; PRINCIPLES §1/§14 name proplang as the brain. Production readout drops its governance-register line.
- 2026-09-19: proplang pinned to its first release, `doctrine-sitting-r1` (= `94fd4eb`, sha256 verified); `make engine` downloads it with curl (no gh auth needed) and refuses a mismatched sha. The ER core is its own public repo (hkaddresses#27); tannen `m5-close` is J2's pin.
- 2026-09-19: J1 steps 1–2. The candidate posterior (`core/posterior.py`), the utility folds and the reliability Betas are local Python, each pinned to the Julia engine's recorded outputs (605 decides, 23 boot Ūs, 102 opening ρ). Channel constants live once in `pricing.py`. Board unchanged. The scope proplang can't take is on record in proplang#29.
- 2026-09-19: J1 step 3a. proplang decides on the ask path: the bridge's `/decide` runs the local posterior, then the engine (booted from decision ⋈ verdict), and `membrane/coarse.py` enacts the act. The world declares `gather-open`, so a closed gather is infeasible rather than a host fallback. No second decider: a down engine replies "unavailable". Retired: the shadow, the categorical world, `ask_client.DAEMON`, the grow latch, interval rows, the narrative lane on the path, `bin/answer-brain`. Board unchanged (it reads recorded runs).
