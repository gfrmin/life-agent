# Placeholder + system path roots that may legitimately appear in tracked text.
# One prefix per line. A tracked filesystem path whose prefix is NOT listed here is
# flagged by .githooks/pii_check.py. NEVER add a personal data root (e.g. a real
# mount like a home/data path) here — this file is public. Real machine prefixes are
# rejected automatically via the environment; they are never enumerated in-tree.
/data
/tmp
/fake
/path/to
/etc
/usr
/var
/opt
/srv
/dev
/proc
/sys
~/.config
~/.cache
~/.local
~/.life-agent
~/.env
~/.msmtprc
# answer-brain capability bridge: a two-segment HTTP route namespace (move-3-design), not a
# filesystem path. `/probe/{recency,subject,authority,corroborate}` trips the path-shape
# heuristic; it is public API surface across code/tests/docs, never personal data.
/probe
# GTD webapp (reach/web): its JSON route namespace (`/api/board`, `/api/tasks/{id}/...`,
# `/api/today/clear`) trips the same path-shape heuristic; public API surface, never personal data.
/api
# Kayak Trips export tool (scripts/kayak_trips_export.js): the vendor's own route namespace
# (`/i/api/trips/trip/v3/allTrips`, `/i/api/trips/event/v1/...`) trips the same heuristic. Third-party
# HTTP routes, not filesystem paths, and they carry no account identifiers — the trip/event ids
# interpolated into them come from the live browser session at runtime, never from tracked text.
/i/api
