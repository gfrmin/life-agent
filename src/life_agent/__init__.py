"""life_agent — the personal life-management agent (composition root).

This package answers verbatim point facts from the owner's documents: evidence shaping over
the pkm content-addressed catalogue, a candidate posterior, the utility posterior, and the
Bayes act (``core/decide.bayes_act``) — see ``ROADMAP.md``. Faculties integrate over
language-neutral seams (MCP / HTTP / CLI), so this Python package never assumes what the
eventual agent-loop spine is written in.
"""
