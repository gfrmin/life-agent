# Conferral — what to build after `r52`'s KILL (the residue class, `RULINGS` §5)

**2026-09-07.** `r51b` read X4 CONFIRMED on a public corpus (`GD-32`); `r52` read Y1 KILL at both band constants
(`GD-35`): pooling the labelled corpora that exist yields 437 gradeable questions → 65–101 band rows against the 385
`GD-28` priced for a two-cell separation, and 14 or 9 ATM-sized corpora would be needed at one pass each. The ruled
queue is empty. This document carries the evidence, the options and their prices so the interview is one question
(`M-17`'s form: a recommendation first, every option priced, nothing opened here).

## Evidence in hand ($0)

- **The binding constraint is unchanged**: labelled band rows. Owner corpus 55 (`r49`, 238 verdicted ticks); ATM-Bench 29 (`r51b`,
  195); the two remaining public corpora would add ≈ 36–55 at one pass each. Nothing in reach pools to 385.
- **The owner's un-verdicted backlog (census 2026-09-07, counts only):** ask-path decisions 879 (latest per id); **856
  carry neither a Claude verdict nor a reaction; 87 of them sit in the 0.7–0.9 band** (6 ask-path band rows are already
  verdicted or reacted). The Claude verdict channel (`core/claude_verdicts.py`) is built and dormant — 180 verdicts, none
  since 2026-07-22 (`GD-28` named its re-supply; offered and not chosen twice, `A-11`, `A-13`).
- **The engine-side demand is filed and corroborated**: proplang#26 (`A-11`) carries `r49`'s table and, since
  2026-09-06, `r51b`'s corroboration at ~1× n on a second corpus; 0 counterparty comments as of 2026-09-07.
- **The A3 differential on the public corpus reads FAIL by under-assertion** once both arms share one grader
  (`GD-34`): the membrane withholds on 68 rows the baseline answers correctly at a bar of 0.900 the pooled `p1`
  (0.86–0.88) never clears — the same pooled shape, the opposite failure to `r49`'s over-assertion.

## Options, priced

1. **Re-supply the Claude verdict channel on the band backlog (Recommended).** Deliberate verdicts (never batch-derived —
   the module's own rule) on the 87 un-verdicted ask-path decisions with leader credence in [0.7, 0.9), then keep the
   channel live at the ask-path rate. **Price:** ≈ 2 h of agent deliberation (a session estimate from `r51b`'s 60-row audit; its wall is not recorded), $0 API;
   owner data read on-machine only. **Yield:** band 55 → 142 (2.6× n), which re-arms `r50`'s family census (its KILL was
   BF 0.21–0.25 at n = 55) — **still 37% of 385**, so this buys a re-read of B's separation question, not a two-cell
   proof. Its own pre-registration (`M-3`): the census rule, the deliberation protocol, the BF bar re-frozen at the new n.
2. **Build the two-corpus corroboration anyway** (LongMemEval + LoCoMo as second KB roots): +239 gradeable ticks of two
   new shapes (chat transcripts, diaries), X4 on each, a second look at `r51b`'s X3c′ cell. **Price:** two ATM-sized builds
   (≈ 500 lines + 30 tests each, one PR each), a $215 pass, ≥ 74 min engine, one review. **Yield:** shape diversity for the
   pooled-prior claim; no supply for §18's bar (`r52`).
3. **Hold for the counterparty**: proplang#26 has the demand and the corroboration; check-ins at a stated cadence; the
   repo's next rung is the engine's. **Price:** $0. **Yield:** none until they answer; the verdict supply keeps accruing
   at ≈ 7 band rows a month.
4. **The §17.6 named builds** — the K-cap (k ≤ 3, 74.3% of recorded traffic inside the 20 s timeout; `r48`) or a move on
   #15 / E3 — each its own pre-registration. **Price:** a build each, $0 to read. **Yield:** progress on the E1 re-earn
   (`GD-25`: gather on 126/126 episodes, `p_argmax` 0.983 vs a 0.991 bar), unrelated to the verdict supply.

Offered and not recommended: re-opening `r50`'s B at today's n (its KILL stands until n moves); another external corpus
search (none with evidence labels and typable answers was found beyond the three).

## RULING

*(open — interview pending)*
