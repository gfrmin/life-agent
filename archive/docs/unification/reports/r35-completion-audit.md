# r35 — the completion audit ($0), and the three unblocking rulings enacted

Commissioned by owner ruling `G-2` (2026-08-31, `RULINGS.md`). **The completion audit is a $0
reconciliation of the four disagreements `r26` recorded rather than resolved**, read against the
standing-ruling register, leaving **one definition of record** for each. It is not a new
instrument, it buys nothing, and it reads no code.

Its consequence is frozen by `G-2`: once it reads, **Arc C (proplang) opens** under
membrane-shadow §18's own bars.

---

## 0. A correction this audit owes first

`RULINGS.md` §4's first version said the completion-audit definition was wholly unsettled and
blocked Arc C. **That was wrong**, and it is corrected in the register rather than edited away.
`r27` (K4) resolved the stage map on **2026-08-28**: Stages 0–4, items 3–5 are Stages 3 and 4,
and "five" is the *stage* count, not an item count. The register's first census read conferral
1's carry-forward and Conferral 2's silence and inferred the state of a third source it never
opened — `M-7` in miniature, on day one, by the instrument built to prevent exactly that.

What was genuinely open was narrower: Stage 2's disposition (deferred pending a measurement that
has since closed), the audit's own definition, and K5's method. All three are now ruled.

## 1. The four disagreements, reconciled

### D-1 · What Stage 0 consisted of — RESOLVED, three riders, all discharged

`ROADMAP.md:219` names three: *"baseline re-record closing q2-036; **this** doc-currency sweep;
a production readout for the carried risks"*. `CLAUDE.md` declares Stage 0 DONE citing two, and
`r26` recorded the third as *"never reported done anywhere"*.

**The deictic resolves it.** "**this** doc-currency sweep" names the ROADMAP revision the phrase
is written in — the rider *is* the act of bringing the roadmap to currency, so it discharged at
the moment it was written and there was never a separate artefact to report. `CLAUDE.md` cites
two because the third is the document doing the citing.

**Definition of record:** Stage 0 = baseline re-record (`r11`) · the doc-currency sweep
(self-discharging, the `ROADMAP.md` revision that names it) · the production readout
(`scripts/production_readout.py`, installed). **DONE.**

### D-2 · DONE item 1's wording — RESOLVED as unrecoverable; the referent stands

The only statement of item 1's text sits inside a *decline* branch of
`appendix-a-conferral.md:43` that was never taken, and the string exists nowhere else.

**Definition of record:** the *referent* is attested and met — the owner signed Appendix A at M7
(2026-08-26, enacted at `r17-collapse-m7.md:131-138`). **The exact wording is unrecoverable and
is not reconstructed**; inventing it would manufacture an authority no text carries. A condition
is identified by what discharges it, not by a quotation nobody can produce.

### D-3 · Which numbering discharges the family stage — MOOT, and recorded as moot

Three labels for one referent: "DONE item 2", "foundations §12 stage 2", "Phase 1.6 item 4 /
Stage 2a".

**Ruling `G-1` dissolves the question rather than answering it.** Stage 2 is **RETIRED from the
programme**: it was specified as *builds* while §12 gives it *gates*, K1 deleted the aggregate
family as "family routing in disguise", and its remainder dies with `/route` at migration M5 —
inside a stage ruled out of the programme. Capability work is continuous and eval-gated under
PRINCIPLES §9 as amended, so it needs no programme stage.

**Definition of record:** all three labels name the retired Stage 2. Nothing discharges it
because nothing has to. The thread transformations may still be built when evidence calls for
them, never as a completion condition.

### D-4 · What opens the proplang migration — RESOLVED by `G-2`

Three statements disagreed and none referenced the others: `ROADMAP.md:221` lists it in the
completion chain; `membrane-shadow.md:1273` says it is **not** a completion condition ("Stages
1–2–4 close without it"); `CLAUDE.md` says it opens "post-M7 after the completion audit", which
was undefined.

**Definition of record:** proplang is **not** a completion condition (membrane-shadow governs —
it is the document that states the migration's own terms). It opens **after this audit**, under
membrane-shadow §18's frozen bars: gated-mandatory, FAIL = iterate never park, a second
same-criterion FAIL stops for a ruling. `ROADMAP.md`'s chain listing is corrected to match;
`CLAUDE.md`'s sentence now points at a defined audit.

## 2. The programme, closed

| Stage | What | Status |
|---|---|---|
| **0** | the three riders | **DONE** (`r11` + the self-discharging sweep) |
| **1** | the collapse ladder M2–M7; Appendix A signed at M7 | **DONE** 2026-08-26 (`r12`–`r17`) |
| **2** | the aggregate and thread families | **RETIRED** 2026-08-31 (`G-1`) |
| **3** | the proplang graduation | **not a completion condition**; opens after this audit (`G-2`) |
| **4** | the MVP exit test | **CLOSED** 2026-08-30 at 69 asks — and read: run 18 puts the typed arm ahead of π\* at P(Δ>0.05)=0.959 |

**The completion programme closes at Stages 0, 1 and 4.** Nothing in it remains open.

## 3. What this audit does NOT close, and says so

**Stage 4's named next question stands, unanswered and unclaimed:** *why a system that measures
better is not the one being reached for.* The gauge fixes `u_abstain = 0`, so the utility model
**cannot represent the cost of not answering** — and declining to pay that cost is exactly what
the owner did. That is a question about the **objective**, which `RULINGS.md` §5 puts beyond
what evidence can settle from inside the system. It is the residue, recorded here so that
"the programme is closed" is never read as "the question is answered".

## 4. Cost

$0. No code read, no model call, no run. Four textual reconciliations and one deictic.
