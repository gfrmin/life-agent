# Dependencies / build
node_modules/
.venv/
__pycache__/
*.pyc
dist/
build/

# Local databases / caches
*.duckdb
*.duckdb.wal
.ocr-cache/

# OS
.DS_Store

# Secrets — these belong in gnome-keyring, never in the repo
.env
*.key
*.pem

# Knowledge base lives OUTSIDE the repo, at $LIFE_AGENT_KB (default
# $HOME/.life-agent/kb) — see docs/kb-schema.md. There is no kb/ in the repo.
# Defensive entries below in case tooling is ever run with data in-tree.
ocr-cache/
eval/questions.yaml
eval/phase0_log.md
FAILURES.md

# local-only: superpowers specs are symlinked to $LIFE_AGENT_KB (out of tree, PII-bearing)
docs/superpowers/

# Throwaway sandbox built by scripts/bootstrap-sample.sh (KB + pkm store + config)
examples/.sandbox/
